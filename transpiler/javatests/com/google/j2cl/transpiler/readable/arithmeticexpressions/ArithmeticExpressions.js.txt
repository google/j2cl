/**
 * Transpiled from
 * third_party/java_src/j2cl/transpiler/javatests/com/google/j2cl/transpiler/readable/arithmeticexpressions/ArithmeticExpressions.java.
 */
goog.module('gen.com.google.j2cl.transpiler.readable.arithmeticexpressions.ArithmeticExpressionsModule');


var Class = goog.require('gen.java.lang.CoreModule').Class;
var Object = goog.require('gen.java.lang.CoreModule').Object;
var Util = goog.require('nativebootstrap.UtilModule').Util;


class ArithmeticExpressions extends Object {
  /**
   * Defines instance fields.
   * @private
   */
  constructor() { super(); }

  /**
   * Runs instance field and block initializers.
   * @private
   */
  $init__com_google_j2cl_transpiler_readable_arithmeticexpressions_ArithmeticExpressions() {
  }

  /**
   * @public
   */
  m_main() {
    let a = 10;
    let b = a++;
    let c = a--;
    let d = ++a;
    let e = --a;
    let f = -a;
    let g = +a;
    let h = ~a;
    let i = 1 + 1 + 2 - 5;
    let j = (1 + 2) * (3 + 4);
    let k = !(1 + 2 + 3 == 4);
    let l = (1 + 2 != 4);
  }

  /**
   * A particular Java constructor as a factory method.
   * @return {!ArithmeticExpressions}
   * @public
   */
  static $create() {
    ArithmeticExpressions.$clinit();
    var instance = new ArithmeticExpressions;
    instance
        .$ctor__com_google_j2cl_transpiler_readable_arithmeticexpressions_ArithmeticExpressions();
    return instance;
  }

  /**
   * Initializes instance fields for a particular Java constructor.
   * @protected
   */
  $ctor__com_google_j2cl_transpiler_readable_arithmeticexpressions_ArithmeticExpressions() {
    this.$ctor__java_lang_Object();
    this.$init__com_google_j2cl_transpiler_readable_arithmeticexpressions_ArithmeticExpressions();
  }

  /**
   * Returns whether the provided instance is an instance of this class.
   * @return {boolean}
   * @public
   */
  static $isInstance(instance) {
    return instance instanceof ArithmeticExpressions;
  }

  /**
   * Returns whether the provided class is or extends this class.
   * @param {Function} classConstructor
   * @return {boolean}
   * @private
   */
  static $isAssignableFrom(classConstructor) {
    return Util.$canCastClass(classConstructor, ArithmeticExpressions);
  }

  /**
   * Runs inline static field initializers.
   * @protected
   */
  static $clinit() { Object.$clinit(); }
}
;


/**
 * @public {Class}
 */
ArithmeticExpressions.$class = Class.$createForClass(
    Util.$generateId('ArithmeticExpressions'),
    Util.$generateId(
        'com.google.j2cl.transpiler.readable.arithmeticexpressions.ArithmeticExpressions'),
    Object.$class,
    Util.$generateId(
        'com.google.j2cl.transpiler.readable.arithmeticexpressions.ArithmeticExpressions'));

/**
 * Export class.
 */
exports.ArithmeticExpressions = ArithmeticExpressions;
