#if(${method.SelfReference.Visibility.isPublic()})
#set ($ctorVisibility = "protected")
#else
#set ($ctorVisibility = $method.SelfReference.Visibility)
#end
  /**
   * A particular Java constructor as a factory method.
   * @return {!$jsDocName}
   * @${method.SelfReference.Visibility}
   */
  static $create() {
    $className.$clinit();
    var instance = new $className;
    instance.$ctor__$mangledTypeName();
    return instance;
  }

  /**
   * Initializes instance fields for a particular Java constructor.
   * @$ctorVisibility
   */
  $ctor__$mangledTypeName() {
    this.$ctor__$superTypeMangledName();
    this.$init__$mangledTypeName();
  }
