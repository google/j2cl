/**
 * @fileoverview Header transpiled from
 * com.google.j2cl.transpiler.readable.packageinfo.Outer$Inner
 *
 * @suppress {lateProvide}
 */
goog.module('readable.packageinfo.Outer.Inner');


// Imports headers for both eager and lazy dependencies to ensure that
// all files are included in the dependency tree.
let _java_lang_Object = goog.require('java.lang.Object');
let _$Util = goog.require('nativebootstrap.Util');
let _Outer = goog.require('readable.packageinfo.Outer');


// Re-exports the implementation.
var Inner = goog.require('readable.packageinfo.Outer.Inner$impl');
exports = Inner;
