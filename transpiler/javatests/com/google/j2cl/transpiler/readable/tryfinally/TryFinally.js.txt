/**
 * Transpiled from
 * third_party/java_src/j2cl/transpiler/javatests/com/google/j2cl/transpiler/readable/tryfinally/TryFinally.java.
 */
goog.module('gen.com.google.j2cl.transpiler.readable.tryfinally.TryFinallyModule');


var Class = goog.require('gen.java.lang.CoreModule').Class;
var Object = goog.require('gen.java.lang.CoreModule').Object;
var $Util = goog.require('nativebootstrap.UtilModule').Util;
var $Asserts = goog.require('vmbootstrap.AssertsModule').Asserts;


class TryFinally extends Object {
  /**
   * Defines instance fields.
   * @private
   */
  constructor() { super(); }

  /**
   * Runs instance field and block initializers.
   * @private
   */
  $init__com_google_j2cl_transpiler_readable_tryfinally_TryFinally() {}

  /**
   * @return {void}
   * @public
   */
  m_main() {
    try {
      $Asserts.$enabled() && $Asserts.$assert(true);
    } finally {
    }
  }

  /**
   * A particular Java constructor as a factory method.
   * @return {!TryFinally}
   * @public
   */
  static $create() {
    TryFinally.$clinit();
    var instance = new TryFinally;
    instance.$ctor__com_google_j2cl_transpiler_readable_tryfinally_TryFinally();
    return instance;
  }

  /**
   * Initializes instance fields for a particular Java constructor.
   * @protected
   */
  $ctor__com_google_j2cl_transpiler_readable_tryfinally_TryFinally() {
    this.$ctor__java_lang_Object();
    this.$init__com_google_j2cl_transpiler_readable_tryfinally_TryFinally();
  }

  /**
   * Returns whether the provided instance is an instance of this class.
   * @return {boolean}
   * @public
   */
  static $isInstance(instance) { return instance instanceof TryFinally; }

  /**
   * Returns whether the provided class is or extends this class.
   * @param {Function} classConstructor
   * @return {boolean}
   * @private
   */
  static $isAssignableFrom(classConstructor) {
    return $Util.$canCastClass(classConstructor, TryFinally);
  }

  /**
   * Runs inline static field initializers.
   * @protected
   */
  static $clinit() { Object.$clinit(); }
}
;


/**
 * @public {Class}
 */
TryFinally.$class = Class.$createForClass(
    $Util.$generateId('TryFinally'),
    $Util.$generateId(
        'com.google.j2cl.transpiler.readable.tryfinally.TryFinally'),
    Object.$class,
    $Util.$generateId(
        'com.google.j2cl.transpiler.readable.tryfinally.TryFinally'));

/**
 * Export class.
 */
exports.TryFinally = TryFinally;
