/**
 * @fileoverview Header transpiled from
 * com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda
 *
 * @suppress {lateProvide}
 */
goog.module('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda');


// Imports headers for both eager and lazy dependencies to ensure that
// all files are included in the dependency tree.
let _java_lang_Object = goog.require('java.lang.Object');
let _$Util = goog.require('nativebootstrap.Util');
let _MyInterface = goog.require('com.google.j2cl.transpiler.readable.simplelambda.MyInterface');
let _SimpleLambda$0foo = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda.$Anonymous0foo');
let _SimpleLambda$1foo = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda.$Anonymous1foo');
let _SimpleLambda$2foo = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda.$Anonymous2foo');
let _SimpleLambda$3foo = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda.$Anonymous3foo');
let _SimpleLambda$4foo = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda.$Anonymous4foo');
let _SimpleLambda$5foo = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda.$Anonymous5foo');
let _SimpleLambda$6foo = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda.$Anonymous6foo');
let _$int = goog.require('vmbootstrap.primitives.$int');


// Re-exports the implementation.
var SimpleLambda = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$impl');
exports = SimpleLambda;
