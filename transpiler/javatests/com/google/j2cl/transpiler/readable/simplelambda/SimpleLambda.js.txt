/**
 * Header transpiled from
 * com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda.
 */
goog.module('gen.com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda');


// Imports headers for both eager and lazy dependencies to ensure that
// all files are included in the dependency tree.
let _Object = goog.require('gen.java.lang.Object');
let _$Util = goog.require('nativebootstrap.Util');
let _MyInterface = goog.require('gen.com.google.j2cl.transpiler.readable.simplelambda.MyInterface');
let _SimpleLambda$lambda$0 = goog.require('gen.com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$lambda$0');
let _SimpleLambda$lambda$1 = goog.require('gen.com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$lambda$1');
let _SimpleLambda$lambda$2 = goog.require('gen.com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$lambda$2');
let _SimpleLambda$lambda$3 = goog.require('gen.com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$lambda$3');
let _SimpleLambda$lambda$4 = goog.require('gen.com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$lambda$4');
let _SimpleLambda$lambda$5 = goog.require('gen.com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$lambda$5');
let _Class = goog.require('gen.java.lang.Class');
let _$int = goog.require('vmbootstrap.primitives.$int');


// Re-exports the implementation.
var SimpleLambda = goog.require('gen.com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$impl');
exports = SimpleLambda;
