/**
 * @fileoverview Header transpiled from
 * com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$5foo
 *
 * @suppress {lateProvide}
 */
goog.module('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$5foo');


// Imports headers for both eager and lazy dependencies to ensure that
// all files are included in the dependency tree.
let _MyInterface = goog.require('com.google.j2cl.transpiler.readable.simplelambda.MyInterface');
let _Object = goog.require('java.lang.Object');
let _$Util = goog.require('nativebootstrap.Util');
let _SimpleLambda = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda');
let _Class = goog.require('java.lang.Class');
let _$int = goog.require('vmbootstrap.primitives.$int');


// Re-exports the implementation.
var SimpleLambda$5foo = goog.require('com.google.j2cl.transpiler.readable.simplelambda.SimpleLambda$5foo$impl');
exports = SimpleLambda$5foo;
