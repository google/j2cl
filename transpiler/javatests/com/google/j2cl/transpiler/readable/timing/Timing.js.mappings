[/**
 * An example that exists to pull in Guava so that JSCompiler's slow type check time of translated
...  }
}] => [super();] "com.google.j2cl.transpiler.readable.timing.Timing.<synthetic: ctor_js>"
[/**
 * An example that exists to pull in Guava so that JSCompiler's slow type check time of translated
...  }
}] => [Timing.$clinit();] "com.google.j2cl.transpiler.readable.timing.Timing.<synthetic: ctor_create>"
[/**
 * An example that exists to pull in Guava so that JSCompiler's slow type check time of translated
...  }
}] => [let $instance = new Timing();] "com.google.j2cl.transpiler.readable.timing.Timing.<synthetic: ctor_create>"
[/**
 * An example that exists to pull in Guava so that JSCompiler's slow type check time of translated
...  }
}] => [$instance.$ctor__com_google_j2cl_transpiler_readable_timing_Timing__();] "com.google.j2cl.transpiler.readable.timing.Timing.<synthetic: ctor_create>"
[/**
 * An example that exists to pull in Guava so that JSCompiler's slow type check time of translated
...  }
}] => [return $instance;] "com.google.j2cl.transpiler.readable.timing.Timing.<synthetic: ctor_create>"
[/**
 * An example that exists to pull in Guava so that JSCompiler's slow type check time of translated
...  }
}] => [this.$ctor__java_lang_Object__();] "com.google.j2cl.transpiler.readable.timing.Timing.<init>"
[/**
 * An example that exists to pull in Guava so that JSCompiler's slow type check time of translated
...  }
}] => [this.$init__com_google_j2cl_transpiler_readable_timing_Timing();] "com.google.j2cl.transpiler.readable.timing.Timing.<init>"
[public static boolean run() {
    // Use both Guava collections and base libraries, just so the source and BUILD validators don't
...    return Objects.equal(fooStrings, barStrings);
  }] => [Timing.$clinit();] "com.google.j2cl.transpiler.readable.timing.Timing.run"
[List<String> fooStrings = Lists.newArrayList("foo");] => [let fooStrings = /**@type {ArrayList<?string>} */ (Lists.m_newArrayList__arrayOf_java_lang_Object(/**@type {!Array<?string>} */ ($Arrays.$init(["foo"], java_lang_String))));] "com.google.j2cl.transpiler.readable.timing.Timing.run"
[fooStrings] => [fooStrings] "fooStrings"
[List<String> barStrings = Lists.newArrayList("bar");] => [let barStrings = /**@type {ArrayList<?string>} */ (Lists.m_newArrayList__arrayOf_java_lang_Object(/**@type {!Array<?string>} */ ($Arrays.$init(["bar"], java_lang_String))));] "com.google.j2cl.transpiler.readable.timing.Timing.run"
[barStrings] => [barStrings] "barStrings"
[return Objects.equal(fooStrings, barStrings);] => [return Objects.m_equal__java_lang_Object__java_lang_Object(fooStrings, barStrings);] "com.google.j2cl.transpiler.readable.timing.Timing.run"
[/**
 * An example that exists to pull in Guava so that JSCompiler's slow type check time of translated
...  }
}] => [java_lang_Object.$clinit();] "com.google.j2cl.transpiler.readable.timing.Timing.<clinit>"
